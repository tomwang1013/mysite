.basic-info.small-font
  span
    img(src=user.avatarUrl() alt='头像' width='24px' height='24px')
  span= user.name
  span= user.business
  span= user.scaleValue()
  span= user.maturityValue()
  span
    a(href=user.url)= user.url
.company-desc
  .head.text-bold 公司介绍：
  .detail.small-font!= user.desc
.jobs
  .head.text-bold 公司发布的所有职位：
  each job in jobs
    .job.flex.small-font
      span
        a(href='/job/' + job.id)= job.title
      span= job.address
      span= job.displaySalary
      span= job.createdAt.toLocaleDateString()
