table
  thead
    tr
      th 职位名称
      th 工作地点
      th 薪资(元/月)
      th 职责
      th 要求
      th 备注
      th 状态
  tbody
    each job in jobs
      tr
        td= job.title
        td= job.address
        td= job.salary
        td= job.duty
        td= job.requirement
        td= job.notes
        td 审核中
